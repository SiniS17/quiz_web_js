{
  "name": "aviation-quiz-app",
  "version": "1.0.0",
  "description": "Aviation Maintenance Quiz App",
  "scripts": {
    "dev": "node scripts/generate-validation-cache.js && next dev -p 5000 -H 0.0.0.0",
    "build": "node scripts/generate-validation-cache.js && next build",
    "start": "next start -p 5000 -H 0.0.0.0"
  },
  "dependencies": {
    "next": "^14.2.29",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
